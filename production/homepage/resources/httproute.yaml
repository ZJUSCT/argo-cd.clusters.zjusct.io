apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
  labels:
    app.kubernetes.io/name: homepage
spec:
  parentRefs:
    - name: envoy-gateway
      namespace: envoy-gateway
  hostnames:
    - clusters.zjusct.io
    - s.zjusct.io
    - clusters.zju.edu.cn
  rules:
    - backendRefs:
        - name: homepage
          port: 3000
