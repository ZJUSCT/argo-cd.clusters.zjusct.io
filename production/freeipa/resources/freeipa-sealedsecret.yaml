---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: freeipa-admin-secrets
  namespace: freeipa
spec:
  encryptedData:
    admin-password: AgA...  # Replace with your sealed admin password
    directory-password: AgA...  # Replace with your sealed directory manager password
  template:
    metadata:
      name: freeipa-admin-secrets
      namespace: freeipa
    type: Opaque
