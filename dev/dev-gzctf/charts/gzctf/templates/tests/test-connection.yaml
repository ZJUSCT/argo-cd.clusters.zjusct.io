apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gzctf.fullname" . }}-test-connection"
  labels:
    {{- include "gzctf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gzctf.fullname" . }}:{{ .Values.gzctf.service.httpPort }}']
  restartPolicy: Never
