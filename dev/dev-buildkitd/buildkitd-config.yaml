apiVersion: v1
kind: ConfigMap
metadata:
  name: buildkitd-config
  namespace: dev-buildkitd
data:
  buildkitd.toml: |
    debug = true

    # Allow self-signed TLS certificates (internal Harbor core service)
    [registry."harbor-core.default.svc.cluster.local"]
      insecure = true
