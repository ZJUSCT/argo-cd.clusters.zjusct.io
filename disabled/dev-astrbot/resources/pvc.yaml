apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: astrbot-data-shared-pvc
  labels:
    app: astrbot-stack
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-filesystem # Uncomment and set your RWX storage class if needed

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: napcat-config-pvc
  labels:
    app: astrbot-stack
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: napcat-qq-pvc
  labels:
    app: astrbot-stack
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard

---
# 持久化 machine-id，保持设备标识不变
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: napcat-machine-id-pvc
  labels:
    app: astrbot-stack
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi  # 只需存储一个 32 字节的文件
  # storageClassName: standard
