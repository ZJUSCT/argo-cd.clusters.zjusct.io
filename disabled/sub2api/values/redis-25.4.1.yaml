architecture: standalone
auth:
  enabled: true
  existingSecret: sub2api-secrets
  existingSecretPasswordKey: redis-password

master:
  persistence:
    size: 1Gi
  resources:
    requests:
      memory: 64Mi
      cpu: 50m
    limits:
      memory: 256Mi
      cpu: 200m
